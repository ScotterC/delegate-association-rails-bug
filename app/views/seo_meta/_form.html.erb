<%= form_for(@seo_metum) do |f| %>
  <% if @seo_metum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@seo_metum.errors.count, "error") %> prohibited this seo_metum from being saved:</h2>

      <ul>
      <% @seo_metum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :browser_title %><br />
    <%= f.text_field :browser_title %>
  </div>
  <div class="field">
    <%= f.label :meta_keywords %><br />
    <%= f.text_field :meta_keywords %>
  </div>
  <div class="field">
    <%= f.label :meta_description %><br />
    <%= f.text_area :meta_description %>
  </div>
  <div class="field">
    <%= f.label :meta_author %><br />
    <%= f.text_field :meta_author %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
